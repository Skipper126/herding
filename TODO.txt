improve agents layout spawning out of window view
put env_data.config into categories
fix setup.py with .cl and .h files
improve README.md
rework benchmark
add barrier env entity
dog movement dependent on rotation
add locked_on_target dogs mode
add spaces info to herding
add gym setups in register_gym
update names.py
make reward counter max work group size independent
make rays_length optional if rendering is used
add herding zone as episode target + render
maybe delete BufferInfo class
add complex sheep movement
add sheep flee distance as env_data.config
rework colors (rays colors etc)
check the thing with herding.seed()
make manual_steering accept command line parameters
check multiline text drawing
